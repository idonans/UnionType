apply plugin: 'maven'

group = 'com.idonans.uniontype'
version = rootProject.ext.bcfg.ver.versionName

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri("../repositoryLocal"))
        }
    }
}